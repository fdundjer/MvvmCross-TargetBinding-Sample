using System;
using UIKit;

namespace HTEC.TargetBinding.iOS.Common
{
    public static class Colors
    {
        public static UIColor DefaultLogoTintColor = new UIColor(red: (nfloat)(52 / 255.0), green: (nfloat)(152 / 255.0),
                    blue: (nfloat)(219 / 255.0), alpha: 1);
    }
}